import Content from "../content";
import Code from "../code";
import typesExample from "./types-example.ts?raw";

<Content>
    <h2 className="subtitle">Types</h2>

    TypeScript is all about types, and it allows you to define your own. Take the example below:
</Content>

<Code language="typescript" id="TypesExample" code={typesExample} noCopy={true} />

<Content>
    It defines a type `Profile` that has one optional attribute, `name`, that must be a string.
</Content>