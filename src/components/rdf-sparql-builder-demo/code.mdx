import demoCode from "./index.tsx?raw";
import Code from "../code";
import CodeStep from "../code/step";

<Code language="tsx" id="RDFSPARQLBuilderDemo" code={demoCode} className="line-numbers">
    <CodeStep lines={["21-24"]} title="Building SELECT query">
        Using the `sparql.select` function we start building our query to get a name.
    </CodeStep>
    <CodeStep lines={["42-45"]} title="Building UPDATE query">
        For this query we start off with the `sparql.deleteQuery` function.
    </CodeStep>
</Code>
