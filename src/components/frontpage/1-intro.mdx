import Content from "../content";
import {BsFillHeartFill} from "react-icons/bs";
import Libraries from "../libraries";

<Content id="intro">
    <h1 className="title" style={{display: "flex", gap: "0.25rem", alignItems: "center"}}>
        <span>RDF + JS = </span>
        <BsFillHeartFill style={{fill: "red"}}/>
    </h1>

    Welcome to this website that documents and reviews various ways you can work with Linked Data (aka
    [RDF](https://www.w3.org/TR/rdf11-primer/), aka semantic data) using JavaScript libraries. I've chosen five
    libraries to showcase how this can be done:
</Content>

<Libraries />